# =========================
# Local Dev Only
# =========================

# 与 app-d 永不冲突的本地端口
PORT=3001

# turbo 并行启动时，禁止抢浏览器焦点
BROWSER=none

# app-e 对应的本地 API
REACT_APP_API_BASE_URL=http://localhost:8081

# 明确标记为本地运行（仅用于 debug / feature flag）
REACT_APP_ENV=local

# CRA 本地开发体验
FAST_REFRESH=true
