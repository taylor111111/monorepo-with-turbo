# =========================
# Local Dev Only
# =========================

# dev server 端口（只用于本地，避免和其他 app 冲突）
PORT=3000

# 是否在浏览器中自动打开
BROWSER=none

# 本地 API mock / dev 后端
REACT_APP_API_BASE_URL=http://localhost:8080

# 本地运行模式标识（只给开发用）
REACT_APP_ENV=local

# 提高本地调试体验
FAST_REFRESH=true
